apiVersion: v1
kind: Service
metadata:
  name: java-demo-service
  namespace: dev
spec:
  selector:
    app: java-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080
  type: NodePort
